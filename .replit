run = "bash -c \"npm install && node server.js\""
entrypoint = "server.js"
modules = ["nodejs-20"]

[[ports]]
localPort = 3000
externalPort = 80

[deployment]
deploymentTarget = "gce"
build = ["sh", "-c", "npm install"]
run = ["sh", "-c", "node server.js"]
